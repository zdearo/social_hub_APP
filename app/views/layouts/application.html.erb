<!DOCTYPE html>
<html data-theme="night">
  <head>
    <title>SocialHubApp</title>
    <meta name="turbo-refresh-method" content="morph">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="turbo-refresh-scroll" content="preserve">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="p-4">
      <nav class="navbar bg-base-300 rounded-box">
        <div class="navbar-start flex-1 px-2 lg:flex-none">
          <a href="/" class="text-lg font-bold">SocialHub.App</a>
        </div>

        <div class="navbar-center hidden lg:flex">
          <ul >
            <li><a href="/feed" class="btn btn-ghost rounded-btn">Feed</a></li>
          </ul>
          
        </div>

        <div class="navbar-end">
        </div>
      </nav>
    </header>

    <main class="container mx-auto p-4">
      <%= yield %>
    </main>
  </body>
</html>
